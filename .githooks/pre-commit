#!/bin/sh
# Fast lint checks before each commit (no pytest).
set -e

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

echo "pre-commit: black --check"
make lint-python

echo "pre-commit: shellcheck"
make lint-shell

echo "pre-commit: lint passed"
